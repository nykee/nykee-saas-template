# Public build-time values. Google client IDs are safe to expose in a browser;
# the matching private client secret belongs in .dev.vars or Wrangler secrets.
VITE_GOOGLE_CLIENT_ID=replace-with-google-oauth-client-id.apps.googleusercontent.com
